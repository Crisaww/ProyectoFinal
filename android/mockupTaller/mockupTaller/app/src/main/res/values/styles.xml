<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MockupTaller" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/my_light_primary</item>
        <item name="colorOnPrimary">@color/my_light_on_primary</item>
        <item name="colorSurface">@color/my_light_surface</item>
        <item name="colorOnSurface">@color/my_light_on_surface</item>
        <!-- Add more theme attributes for the light theme here. -->
    </style>
    <!-- Dark Theme -->
    <style name="Theme.MockupTaller.Dark" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/my_dark_primary</item>
        <item name="colorOnPrimary">@color/my_dark_on_primary</item>
        <item name="colorSurface">@color/my_dark_surface</item>
        <item name="colorOnSurface">@color/my_dark_on_surface</item>
        <!-- Add more theme attributes for the dark theme here. -->
    </style>

    <!-- Light Theme -->
    <style name="Theme.MockupTaller.Light" parent="Base.Theme.MockupTaller">
        <!-- Customize your light theme here if needed. -->
        <item name="colorPrimary">@color/my_light_primary</item>
        <item name="colorOnPrimary">@color/my_light_on_primary</item>
        <item name="colorSurface">@color/my_light_surface</item>
        <item name="colorOnSurface">@color/my_light_on_surface</item>
        <!-- Add more theme attributes for the light theme here if needed. -->
    </style>
    <!-- Custom Theme with no all caps text -->
    <style name="Theme.custom" parent="Base.Theme.MockupTaller">
        <item name="android:textAllCaps">false</item>
    </style>

    <style name="CustomCheckbox01" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:colorControlNormal">#7a7a7a</item>
        <item name="android:colorAccent">#00FF00</item>
        <item name="android:colorControlHighlight">#00FF00</item>
    </style>

</resources>